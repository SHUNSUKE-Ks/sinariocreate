# 本日のTODO（TrialRogue）

最終更新: 2025-09-06
対象: `src/GameCollections/TrialRogue/*`

---

## 今日のゴール
- キーマップの profiles 追加と自動バインド（Battle へ反映）
- 敵の攻撃ターンと勝敗表示（Game Over / Stage Clear）
- `generateMap` グリッド適用（壁侵入不可の地形判定）

---

## 優先タスク（実装）
- profiles の設計と JSONC 反映
  - `src/GameCollections/TrialRogue/data/keymaps/combat_keymap.jsonc`
  - 追加案: `profiles.pc`, `profiles.android` を用意
    - 例: `profiles.pc` = { "move.n": ["ArrowUp","w"], "move.s": ["ArrowDown","s"], "move.w": ["ArrowLeft","a"], "move.e": ["ArrowRight","d"], "move.nw":["q"], "move.ne":["e"], "move.sw":["z"], "move.se":["c"], "wait":[" ","Enter"] }
- Battle の入力自動バインド
  - `screens/BattleScreen.jsx` で JSONC を `?raw` 読み→コメント除去→`JSON.parse`
  - `profiles.pc` を選択し、`key → actionId` と `actionId → payload(dx,dy)` をマップ化
  - 既存の `keyToDelta` を profiles 駆動に置換
- 敵の攻撃ターンと勝敗
  - プレイヤー行動後に敵手番: 隣接時は `P_HP -= max(0, E_AT - P_DF)`（0未満切り捨て）
  - `P_HP <= 0` で Game Over オーバーレイ表示・入力ロック
  - `E_HP <= 0` で Stage Clear オーバーレイ表示・入力ロック
- generateMap の地形判定
  - `generateMap/scripts/*` から `generateDungeon` を呼び出しグリッド生成
  - `wall` タイプ（`passable=false`）は侵入不可
  - プレイヤー/敵/アイテムの初期配置は `floor` 上へスナップ
  - 簡易描画: 背景にタイル色分け（floor/wall）→現行エンティティを上描画

---

## 画面・UX
- ロード画面（`LoadingScreen.jsx`）
  - profiles の存在を明示（選択肢: PC/Android 表示だけ先に行う）
  - クリックでバトルへ遷移
- バトル画面（`BattleScreen.jsx`）
  - HUD: 左上に `HP`、右上に `小ログ（3行）`
  - 勝敗時のオーバーレイ: クリックで StageSelect に戻る

---

## 受け入れ基準（Acceptance Criteria）
- JSONC の `profiles.pc` を変更すると、バトルの入力挙動が即座に反映される
- 壁タイルに対して、プレイヤー/敵ともに侵入不可である
- 隣接時の敵攻撃で HP が減少し、HP0 で Game Over 表示になる
- 敵HP0 で Stage Clear 表示になる

---

## 作業対象ファイル
- `src/GameCollections/TrialRogue/data/keymaps/combat_keymap.jsonc`
- `src/GameCollections/TrialRogue/screens/LoadingScreen.jsx`
- `src/GameCollections/TrialRogue/screens/BattleScreen.jsx`
- 必要に応じて: `src/GameCollections/TrialRogue/generateMap/scripts/*`

---

## 実装メモ
- JSONC パース: `raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/(^|\n)\s*\/\/.*(?=\n|$)/g,'$1')`
- プロファイル解決: `const keysById = profiles.pc; const idByKey = invert(keysById)`
- 地形: `tile.passable === false` を壁とみなす
- 初期配置: 乱数 or 既定座標→床タイルへスナップ（衝突回避）

---

## チェックリスト
- [ ] JSONC に `profiles.pc` を追加
- [ ] Battle で profiles を読み込み、キー自動バインド
- [ ] 敵攻撃ターンを実装
- [ ] ゲームクリア/オーバー表示を実装
- [ ] generateMap グリッドを適用し壁判定
- [ ] 簡易HUD/ログ追加

